<UserControl x:Class="JUtils.view.TitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JUtils.view"
             MouseLeftButtonDown="UserControl_MouseLeftButtonDown"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="800">
    <Grid>
        <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="Transparent" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnMaximize" Content="⏹️" Width="30" FontSize="15"  Height="30" Background="Transparent" BorderBrush="Transparent" Click="btnMaximize_Click"/>
            <Button x:Name="btnMinimize" Content="-" Width="30" FontSize="15"  Height="30" Background="Transparent" BorderBrush="Transparent" Click="btnMinimize_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
